### Behind the scene working of Iteration in Python.

1). In the working of the iteration, We have mainly 3 major element's which are responsible for the success of iteration in the python.
    
    i) iteration tool-: It contain `iterators` like (for, while, do-while) and Comprehension's like, (list, dictionary, set... etc).

    ii) iterable objects-: The object on which we are using the loop.

    iii) __next__ or next()-: 


At first phase `Iteration tool` send the request to `Iteraable object` for continuing the iteration. And sends and `iter()`/( iter function ) 